# ========================================================
# ATENCAO: Este arquivo (.env.example na raiz) foi SUBSTITUIDO por um
# redirecionador. O arquivo canonico de exemplo agora e':
#
#     application/.env.example
#
# Copie este arquivo canonico para application/.env:
#     cp application/.env.example application/.env
# E ajuste os valores para seu ambiente.
#
# Este redirecionador e' mantido apenas para nao quebrar documentacao
# antiga que possa referenciar este caminho. Em versoes futuras podera
# ser removido sem impacto.
# ========================================================

APP_ENV=development
APP_DEBUG=true

# Para TODAS as outras variaveis, consulte: application/.env.example
